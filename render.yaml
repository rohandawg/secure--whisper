services:
  # Backend API Service
  - type: web
    name: securechat-backend
    env: node
    buildCommand: cd server && npm install
    startCommand: cd server && npm start
    envVars:
      - key: JWT_SECRET
        generateValue: true
      - key: ENCRYPTION_KEY
        generateValue: true
      - key: PORT
        value: 4000
      - key: MONGODB_URI
        fromDatabase:
          name: securechat-db
          property: connectionString
  
  # Frontend Web Service
  - type: web
    name: securechat-frontend
    env: node
    buildCommand: npm install && npm run build
    startCommand: npx serve -s dist -l 10000
    # Note: Set VITE_API_URL manually in Render dashboard after backend is deployed
    # Format: https://securechat-backend.onrender.com

databases:
  - name: securechat-db
    databaseName: mingleup
    plan: free